## What changed

<!-- Short paragraphs, plain words. What and why, not a diff summary. -->

## How it was checked

- [ ] `uv run ruff check --fix . && uv run ruff format . && uv run ty check . && uv run pytest` in `src/`
- [ ] Ran the affected script or notebook against a live backend, or said why not
- [ ] Docs updated (English first, then the translated twins)
